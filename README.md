The frontend(react) code is inside the `frontend` folder.  
Clone The Repo, open the folder, and run these commands in the terminal.  
```npm i```  
```cd frontend```  
```npm i```  
To run both apps(backend + frontend), type `npm run dev` in both folders.
